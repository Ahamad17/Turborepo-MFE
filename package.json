{
  "name": "turborepo-mfe",
  "private": true,
  "scripts": {
    "start:prod": "npm run build && cross-env NODE_ENV=production npm run start -w server --max-http-header-size=80000",
    "start:dev": "cross-env NODE_OPTIONS='--inspect' NODE_ENV=local npm run dev -w server --max-http-header-size=80000",
    "build": "turbo build",
    "dev": "turbo dev",
    "lint": "turbo lint",
    "format": "prettier --write \"**/*.{ts,tsx,md}\""
  },
  "devDependencies": {
    "@turbo/gen": "^2.3.1",
    "prettier": "^3.2.5",
    "turbo": "^2.3.1",
    "typescript": "^5.5.4"
  },
  "engines": {
    "node": ">=18"
  },
  "packageManager": "yarn@1.22.22",
  "workspaces": [
    "apps/*",
    "packages/*"
  ]
}
